language: node_js
node_js:
- 8
script:
- yarn
- yarn build
dist: xenial
deploy:
  - edge:
      branch: v1.8.47
    provider: pages
    skip_cleanup: true
    github_token: $GITHUB_KEY  # Set in the settings page of your repository, as a secure variable
    keep_history: true
    local_dir: dist
    on:
      branch: master

